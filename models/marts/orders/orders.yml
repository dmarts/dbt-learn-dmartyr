version: 2
models:
  - name: orders
    description: This model lists orders placed
    columns:
      - name: order_id
        description: Primary key
        tests:
          - not_null
      - name: customer_id
        tests:
          - relationships:
              to: ref('stg_customers')
              field: customer_id